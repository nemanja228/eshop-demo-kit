This repo contains an approved plan: `plan/spec.md` and `plan/tasklist.md`.

Implement TASK {N} from `plan/tasklist.md`, and only that task.

Rules:

1. Read `plan/spec.md` and `plan/tasklist.md` first.
2. Stay inside task {N}'s scope. If the task turns out to be impossible as scoped, or you
   discover something that changes the plan, STOP implementing, record what you found in
   `plan/spec.md` (a short "Discovered during task {N}" note), adjust `plan/tasklist.md`
   if needed, and tell me before continuing.
3. Definition of done: the task's own done-check, plus the repo definition of done (build
   succeeds, full test suite passes). Run both and show the real results.
4. When done: mark task {N} complete in `plan/tasklist.md` with a one-line note of
   anything noteworthy, and commit the work as a single commit
   (message: "task {N}: <short title>").
